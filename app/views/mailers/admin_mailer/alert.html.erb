<div class="content-container">
  <div class="content">
    <p>Greetings,</p>
    <p>There's a new alert &mdash; check out the details below:</p>
    <ul>
      <li>Type: <%= @type %></li>
      <li>Message: <%= @message %></li>
      <li>
        Current User:
        <% if @current_user.present? %>
          <ul>
            <li>ID: <%= @current_user.id %></li>
            <li>Email: <%= @current_user.email %></li>
            <li>Name: <%= @current_user.first_name %></li>
          </ul>
        <% else %>
          n/a
        <% end %>
      </li>
      <li>
        Any additional data:
        <% if @additional_data.empty? %>
          n/a
        <% else %>
          <ul>
            <% @additional_data.each do |key, value| %>
              <li><%= key %> => <%= value %></li>
            <% end %>
          </ul>
        <% end %>
      </li>
    </ul>
    <p>Please respond accordingly.</p>
    <p>Keep making progress; don't stop; you got this 🙌🏾💪🏾🙏🏾.</p>
    <p>
      To your success,
      <br>
      🤖 Rosey
    </p>
  </div>
</div>
