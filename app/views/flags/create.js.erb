var commentID = "<%= @comment.id %>";
var $comment = $(".comments").find(`[data-comment-id='${commentID}']`);
var $flagCommentLink = $comment.find(".flag-comment-link").first();

$flagCommentLink.replaceWith(removeFlagLink());

// Replace with partial to DRY this up from links in _link_actions.html.erb
function removeFlagLink() {
  return $("<a/>")
    .addClass("card-link text-decoration-none bg-primary p-1 bg-opacity-10 rounded remove-flag-comment-link")
    .attr("href", "/comments/" + commentID + "/flags")
    .attr("data-method", "delete")
    .attr("data-remote", "true")
    .attr("rel", "nofollow")
    .text("🚨 Report");
}