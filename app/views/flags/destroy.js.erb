var reactionableID = "<%= @comment.id %>";
var $comment = $(".comments").find(`[data-reactionable-id='${reactionableID}']`);
var $removeFlagCommentLink = $comment.find(".remove-flag-comment-link").first();

$removeFlagCommentLink.replaceWith(flagCommentLink());

// Replace with partial to DRY this up from links in _link_actions.html.erb
function flagCommentLink() {
  return $("<a/>")
    .addClass("card-link link-danger text-decoration-none flag-comment-link")
    .attr("href", "/comments/" + reactionableID + "/flags")
    .attr("data-method", "post")
    .attr("data-remote", "true")
    .attr("rel", "nofollow")
    .text("🚨 Report");
}