<%= form_for [comment, Comment.new], url: comment_replies_path(comment), remote: true do |form| %>
  <%= form.text_area :body,
                      class: "form-control form-control-lg",
                      placeholder: "Reply to #{comment.user.first_name}" %>
<% end %>
