<% if object.errors.any? %>
  <div class="text-danger">
    <p class="lead">Please fix the following <%= pluralize(object.errors.count, 'error') %>:</p>

    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>