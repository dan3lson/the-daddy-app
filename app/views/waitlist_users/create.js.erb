var $container = $(".join-waitlist");
var $footer = $(".footer");
var $whyJoinSection = $(".why-join");
var joinedPartial = "<%= j render(partial: 'joined', locals: { first_name: @waitlist_user.first_name, referral_code: @waitlist_user.referral_code }) %>";

$container.empty();
$whyJoinSection.hide();
$footer.hide();
$container.hide().html(joinedPartial).fadeIn(400);
