<%= form_with(model: waitlist_user, local: true) do |form| %>
  <% if waitlist_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(waitlist_user.errors.count, "error") %> prohibited this waitlist_user from being saved:</h2>

      <ul>
        <% waitlist_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :daddy_type %>
    <%= form.select(:daddy_type, options_for_select(WaitlistUser.daddy_types)) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
